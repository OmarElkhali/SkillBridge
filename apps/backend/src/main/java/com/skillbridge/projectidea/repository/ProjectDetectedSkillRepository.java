package com.skillbridge.projectidea.repository;

import com.skillbridge.projectidea.entity.ProjectDetectedSkill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectDetectedSkillRepository extends JpaRepository<ProjectDetectedSkill, Long> {
}
